# Умный бот: что делать дальше

Продолжение `SMART_BOT_PLAN.md` / `SMART_BOT_ISSUES.md` (этапы 1–5 закрыты). Здесь — что выяснили
после запуска, как измерять пользу от доработок и в каком порядке их делать.

## 1. Что выяснили (измерения, не догадки)

**Сила против простого бота** — доля «дурака» у умного, 2000 партий на конфигурацию,
50 % = равенство, погрешность ≈ ±1 п.п.

| Игроков | 24 карты | 36 карт | 52 карты |
|---|---|---|---|
| 2 | 32,7 % | 32,8 % | 38,8 % |
| 3 | 35,7 % | 40,0 % | 42,0 % |
| 4 | 52,1 % | 43,2 % | 44,4 % |
| 6 | — | — | 50,1 % |

Бот силён в дуэли, на 4+ игроках слабо лучше простого, а 24×4 не лучше вовсе.

**Почему так**
1. **Правила боту не передаются.** `getState()` не отдаёт число игроков, `throwInPolicy`, перевод,
   лимиты стола; в `smartBot.js` их нет. Кто может подкидывать, бот не знает.
2. **Память почти не влияет на решения.** Умный против умного без памяти: 46,5 % на 24×2,
   а на 36×2, 36×4 и 52×4 — 50 % (то есть нет эффекта). Преимущество над простым ботом даёт
   сберегание козырей и разгрузка парами, а не подсчёт карт.
3. **Знание руки соперника почти не используется.** В дуэли примерно в 30–40 % решений рука
   соперника известна точно. Политика пользуется этим только для «добивания» при ≤ 2 картах;
   вероятности и признаки «нет масти» (`probabilityOpponentHas`, `voidSuits`) не вызываются.
4. **Эвристики подбирались на 24×2.** На 4 игроках часть из них ведёт себя иначе
   (`abProfile`, 36×4: без `avoidBurningBigTrump` на ≈ 2 п.п. лучше; `dumpPairs` даёт +3,2 п.п.
   на 24×2 и 0 на 36×4).
5. **Проверенная гипотеза, которая не подтвердилась:** грубое правило «без козырей брать сразу,
   если ждут подкидывания» на 6000 партий в шести конфигурациях даёт 49–51 % (шум); без условия
   «нет козырей» сильно вредит (66–80 %). Значит, здесь нужна вероятностная оценка, а не порог.

## 2. Как измерять пользу от правок

**Матрица конфигураций** (игроки × колода), `throwInPolicy = all` — основная:
`2×{24,36,52}`, `3×{24,36,52}`, `4×{24,36,52}`, `5×{36,52}`, `6×{36,52}`. Политики `neighbors` и
`attackerOnly` — выборочно (например 4×36), чтобы убедиться, что адаптация к правилам работает.

**Метод.** Новая версия против текущей, места чередуются (как в `botMatch`). Точность:
`SE ≈ √(p(1−p)/n)`, то есть ≈ 1 п.п. на 2500 партий и **≈ 0,5 п.п. на 10 000** — это рабочая
выборка на конфигурацию. Парные раздачи (те же карты, места меняются) точность **не улучшают**
(SE 0,61 → 0,61 п.п., замерено), поэтому не усложняем.

**Критерий приёмки правки.** В целевых конфигурациях доля «дурака» у новой версии ≤ 48 %
(значимо лучше), во всех остальных ≤ 51 % (не хуже в пределах шума). Пока правка не проходит —
она не включается по умолчанию.

**Сколько занимает** (одно ядро; на вашей машине, вероятно, быстрее):

| Конфигурация | Скорость | 10 000 партий |
|---|---|---|
| 24×2 | ≈ 2250 партий/с | ≈ 5 с |
| 36×4 | ≈ 870 партий/с | ≈ 12 с |
| 52×6 | ≈ 390 партий/с | ≈ 25 с |

Вся матрица из ~14 конфигураций — **порядка 3–5 минут**. Это годится для ручного запуска и
ночной проверки; в обычном CI гоняем только короткий контроль (легальность и отсутствие ошибок).
Оговорка: решатель эндшпиля (этап 3) замедлит партии, его бюджет времени задаём отдельно.

Результаты складываем в `bench/baseline.json`, чтобы каждое следующее изменение сравнивалось
с зафиксированной линией, а не с памятью.

**Критерий проверяет инструмент, а не человек.** Пороги 48 % / 51 % зашиты в `scripts/evalBots.js`
(этап 1): целевые конфигурации задаются флагом `--target=...`, и при невыполнении критерия прогон
завершается ненулевым кодом возврата. Так правку нельзя «принять на глаз», и ночной прогон не
требует читать таблицу глазами.

**Воспроизводимость.** Для baseline прогон должен быть детерминированным: `scripts/evalBots.js`
использует seeded-PRNG и передаёт его четвёртым аргументом в `new DurakGame(players, rules, rng)`
(движок уже принимает генератор — менять `src/game.js` для этого не нужно). Существующие
`src/cli/botMatch.js` и `scripts/abProfile.js` передают `Math.random`, поэтому годятся для оценки,
но не для фиксации линии.

## 3. Этапы

| # | Этап | Что даёт | Размер (оценка) | Issue |
|---|---|---|---|---|
| 0 | ✅ Клиент собирается из `src/` (`npm run build-client`) | нет дублей движка/ботов; правки бота сразу попадают в браузер | сделано | — |
| 1 | Прогонщик матрицы `scripts/evalBots.js` | воспроизводимая оценка: матрица, доверительные интервалы, JSON, сравнение с baseline | ≈ 150 строк | [#46](https://github.com/mlaa4ml/durakProekt/issues/46) |
| 2 | Правила игры — боту (`state.rules`: игроков, `throwInPolicy`, перевод, лимиты) | без этого нельзя адаптироваться к варианту игры; руки не раскрываются, тест «не подглядывает» остаётся | ≈ 30 строк + тесты | [#47](https://github.com/mlaa4ml/durakProekt/issues/47) |
| 3 | Точный расчёт эндшпиля при известных руках (сначала дуэль) | самый большой нетронутый резерв: «ходить так, чтобы он взял» | ≈ 250 строк, `src/bots/endgame.js` | [#48](https://github.com/mlaa4ml/durakProekt/issues/48) |
| 4 | Вероятности из памяти в середине партии (что бьёт соперник, каких мастей у него нет) | атака и решение «брать или отбиваться» не вслепую | ≈ 150 строк | [#49](https://github.com/mlaa4ml/durakProekt/issues/49) |
| 5 | Профили по варианту игры (2 / 3–4 / 5–6 игроков) и перекалибровка флагов | лечит слабость на 4+ игроках, особенно 24×4 | ≈ 100 строк + таблицы A/B | [#50](https://github.com/mlaa4ml/durakProekt/issues/50) |

Каждый этап — отдельный PR со своей таблицей до/после по матрице и обязательным прогоном
`npm test`. Порядок 1 → 2 → 3 → 4 → 5: сначала линейка, потом правки по убыванию ожидаемой пользы.

### 3.1. Зависимости и целевые конфигурации

Issue этапов 1–5 заведены по [issue #45](https://github.com/mlaa4ml/durakProekt/issues/45);
этот документ — источник правды для их текстов. Следующий этап не начинается, пока предыдущий
не принят (то же правило, что действовало для этапов 1–7 в `SMART_BOT_ISSUES.md`).

| Этап | Зависит от | Целевые конфигурации (`--target`) | Что считаем успехом |
|---|---|---|---|
| 1 | — | — | линейка не врёт: `simple` против `simple` даёт ~50 % ± CI, прогон с одним seed повторяем |
| 2 | 1 | — | данные доведены, сила бота **не изменилась** (все конфигурации ≤ 51 %) |
| 3 | 1, 2 | `2×{24,36,52}` | дуэльный эндшпиль считается точно |
| 4 | 1, 2, 3 | `3×{24,36,52}`, `4×36`, `4×52` | середина партии играется не вслепую |
| 5 | 1, 2, 3, 4 | `4×24`, `4×36`, `4×52`, `6×52` | закрыта слабость на 4+ игроках, дуэль не деградировала |

Этап 2 — единственный без целевых конфигураций: он ничего не улучшает сам по себе, только
доставляет боту данные, поэтому его приёмка — «не хуже в пределах шума» плюс тесты.

### 3.2. Правила, общие для всех этапов

Повторяются в каждой issue, чтобы их не приходилось искать:

* работа в отдельной ветке, PR трогает **только файлы своего этапа**;
* `simpleBotDecide` из `src/bots/simpleBot.js` **не меняется**: на нём висят `src/cli/simulate.js`,
  `src/cli/playVerbose.js`, `scripts/smoke.js`, `server/rooms.js` и локальный клиент;
* бот получает **только** `game.getState(botId)`; всё знание о чужих картах выводится
  `CardTracker` из публично наблюдаемых событий;
* клиент правится **только** в `client/template.html`, `visual/index.html` и `docs/index.html` —
  вывод `npm run build-client` и обязаны остаться идентичными;
* `npm test` и CI зелёные; к PR прикладывается таблица «до/после» по матрице;
* каждое новое эвристическое правило прячется за именованным флагом профиля и подтверждается
  A/B-прогоном; правило, не прошедшее критерий, по умолчанию **выключается** — как сейчас
  `holdHighCardsWhileTalon: false` в `src/bots/smartBot.js`.

## 4. Размер кода и поддержка

Бот и движок правятся **только в `src/`**, интерфейс — в `client/template.html`. Клиентские файлы
`visual/index.html` и `docs/index.html` — вывод сборки, их размер поддержку не усложняет.
Оценка прироста от этапов 1–5 — порядка 500–700 строк в `src/` и `scripts/` (плюс тесты), а не тысячи:
основной объём клиента — это интерфейс, а не бот. Каждый новый модуль бота держим в пределах
~300 строк и с тестами в `test/`. Если шаблон интерфейса (~2100 строк) станет неудобным, его можно
разрезать на файлы тем же сборщиком — но пока в этом нет необходимости.

## 5. Переключение в игре

Пользователю остаются два уровня — «простой» и «умный». Подстройка под вариант игры (число
игроков, колода, кто подкидывает) — **автоматическая внутри умного бота** (этапы 2 и 5), без
отдельных переключателей. Уровень фиксируется на партию и меняется со следующей — так
накопленная память бота не ломается. Если понадобится, следующим шагом можно разрешить разный
уровень для каждого бота за столом.
